name: ACT Backend Tests (float32)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  PYTHONPATH: ${{ github.workspace }}
  # Optional: make TF deterministic-ish and reduce noisy logs
  TF_ENABLE_ONEDNN_OPTS: "0"
  # Enable CI mode for lightweight installs
  ACT_CI_MODE: "true"

jobs:
  backend-float32:
    name: Backend Tests (float32)
    runs-on: ubuntu-latest
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/hostedtoolcache
          df -h

      - name: Clean pip cache
        run: |
          pip cache purge || true

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision onnx onnx2torch tensorflow pandas numpy pyyaml tqdm pytest psutil transformers scikit-learn

      # Network Generation & Basic Tests

      - name: Generate Networks (float32)
        run: |
          cd ${{ github.workspace }}
          python -m act.back_end --generate --device cpu --dtype float32

      - name: Run Serialization Tests (float32)
        run: |
          cd ${{ github.workspace }}
          python -m act.back_end --test-serialization --device cpu --dtype float32

      - name: Run ACT2Torch Tests (float32)
        run: |
          cd ${{ github.workspace }}
          python -m act.pipeline --verify act2torch --device cpu --dtype float32

      # Level 1: Counterexample/Soundness Validation

      - name: Run Counterexample Validation (float32)
        run: |
          cd ${{ github.workspace }}
          python -m act.pipeline --validate-verifier --mode counterexample --device cpu --dtype float32

      # Level 2: Bounds Validation (per TF mode)

      - name: Run Bounds Validation (float32, interval)
        run: |
          cd ${{ github.workspace }}
          python -m act.pipeline --validate-verifier --mode bounds --device cpu --dtype float32 --tf-modes interval

      - name: Run Bounds Validation (float32, hybridz)
        run: |
          cd ${{ github.workspace }}
          python -m act.pipeline --validate-verifier --mode bounds --device cpu --dtype float32 --tf-modes hybridz

      - name: Run Bounds Validation (float32, dual)
        run: |
          cd ${{ github.workspace }}
          python -m act.pipeline --validate-verifier --mode bounds --device cpu --dtype float32 --tf-modes dual
